# Dashboard Sidebar Layout

## What
Restructure the dashboard into a sidebar + main body layout. The sidebar acts as navigation, with the user's profile card (avatar, email, sign-out) at the top and route links below. Only "Dashboard" route for now. Main body area is empty placeholder.

## Who
Authenticated users navigating the dashboard.

## Key Interactions
1. Sidebar fixed on the left, full height, dark themed
2. Profile card at top: user avatar, email, sign-out button
3. Navigation links below profile: "Dashboard" (active state)
4. Main content area to the right — empty for now
5. All existing data-testid attributes preserved
